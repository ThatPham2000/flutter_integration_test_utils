name: flutter_integration_test_utils
description: Flutter integration test utils.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.1.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_test:
    sdk: flutter
  meta:

dev_dependencies:
  flutter_lints: ^2.0.0

        
    
    
                                      